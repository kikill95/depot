= simple_form_for '', url: params[:slug] ? categories_path(params[:slug]) : root_path, remote: true, method: 'get' do |f|
 p
  = f.input :search, input_html: { value: params[:search] }
  = f.button :button, :search, class: 'btn-primary'
